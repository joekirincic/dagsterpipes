# Serialize an R object to Dagster-compatible JSON

Serialize an R object to Dagster-compatible JSON

## Usage

``` r
json_serialize(obj)
```

## Arguments

- obj:

  An R object to serialize.

## Value

A JSON string.
