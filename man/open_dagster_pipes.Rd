% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open.R
\name{open_dagster_pipes}
\alias{open_dagster_pipes}
\title{Open a Dagster Pipes session}
\usage{
open_dagster_pipes()
}
\value{
A \link{PipesContext} or \link{NullPipesContext} object.
}
\description{
Reads bootstrap parameters from environment variables, initializes the
message channel, and returns a \link{PipesContext} ready to use. When not running
under Dagster (environment variables absent), returns a no-op
\link{NullPipesContext} that logs to the console and silently ignores reports.
}
\examples{
\dontrun{
ctx <- open_dagster_pipes()

threshold <- ctx$get_extra("threshold")
ctx$log(sprintf("Using threshold = \%s", threshold), level = "INFO")

# ... do work ...

ctx$report_asset_materialization(
  metadata = list(
    row_count = pipes_metadata_value(1000L, "int")
  )
)
ctx$close()
}
}
